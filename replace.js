const fs = require('fs'); let t=fs.readFileSync('app/admin/page.tsx', 'utf8'); let s = t.indexOf('<div key={subject} className=\"space-y-4\">'); let e = t.indexOf('</div>\n                  </div>\n                ))}'); if(s > -1 && e > -1) { t = t.substring(0, s) + '<SubjectGroup key={subject} subject={subject} notes={notes} onOpenFile={handleOpenFile} onEditNote={setEditingNote} onDeleteNote={handleDelete} />\n                ' + t.substring(e + 43); fs.writeFileSync('app/admin/page.tsx', t); console.log('Replaced'); } else { console.log('Not found', s, e); }
